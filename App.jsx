// Make sure if i need to add the import statments for the React and ReactDOM Lines

import Container from "./Container.jsx"

export default function App() {
  return (
    <div>
      <Container />
    </div>
  )
}